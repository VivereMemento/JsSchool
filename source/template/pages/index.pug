extends ../_template

block variables
	- var title = 'Home'

block content
	main.main
		- for (var i = 1; i <= 10; i++)
			div.folder-list Folder #{i}
				- for (var y = 1; y <= 2; y++)
					div.folder-list__item Inner folder #{y}
		